{
  "name": "v56project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "start": "npm run dev"
  },
  "dependencies": {
    "concurrently": "^7.0.0",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
